node_modules
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules

# Build outputs and cache
/.next/
/out/

# Local Environment Variables
# Contains sensitive keys and environment-specific settings.
# NEVER commit these!
.env*.local

# Production Environment Variables
# Often handled by hosting platform, but good practice to ignore if storing secrets.
.env.production

# Test Environment Variables
.env.test

# Optional: Other .env files if you use them, uncomment if needed
# .env

# TypeScript cache
*.tsbuildinfo

# Next.js static export cache
.next/cache

# Turborepo cache (if using a monorepo with Turborepo)
.turbo

# ESLint cache
.eslintcache

# Optional: Editor/IDE directories
.vscode/      # Unless you want to share specific launch/settings configs
.idea/        # For JetBrains IDEs

# Optional: Local Database Files (like SQLite)
# Good practice even if using a cloud DB now
*.db
*.db-journal
*.sqlite
*.sqlite3
*.sqlitedb

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Optional: Coverage reports
coverage/
.nyc_output/

# Optional: Compiled binary files
*.exe
*.dll
*.so
*.dylib

# Optional: Compressed files
*.zip
*.tar.*
*.gz

# Optional: Uploaded files / Temp directories
/uploads/
/temp/